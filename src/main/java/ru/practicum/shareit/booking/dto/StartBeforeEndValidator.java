package ru.practicum.shareit.booking.dto;

/*public class StartBeforeEndValidator implements ConstraintValidator<BookingDto, List<Date>> {

    @Override
    public boolean isValid(List<Date> dates, ConstraintValidatorContext constraintValidatorContext) {
        if(dates == null) {
            return false;
        }
        return dates.get(0).before(dates.get(1));
    }
    }
}*/
